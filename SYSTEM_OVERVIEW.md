# System Overview: Beauty Salon

## Project Metadata
- **Business Name:** Beauty Salon (Antelope Valley)
- **Primary Domain:** [https://d33g0vhemk77jl.cloudfront.net](https://d33g0vhemk77jl.cloudfront.net)
- **Repositories:** `Beauty-Salon` (Git)
- **Infrastructure:** AWS S3 + CloudFront (Static Hosting)

## Architecture
This project uses the **Workflow Factory V3** architecture. It is a pure static site (HTML/CSS/JS) designed for speed, security, and low maintenance.

### Directory Structure
- `index.html`: The main homepage (Landing).
- `services/`: Directory for individual service pillars.
    - `hair-services/`: Haircuts & Styling.
    - `coloring/`: Balayage & Color.
    - `treatments/`: Hair repair.
    - `makeup/`: Event makeup.
    - `lashes-brows/`: Beauty bar.
    - `skincare/`: Facials.
- `blog/`: The Content Factory (Jekyll-style static generation).
    - `index.html`: The News Hub.
- `factory/`: The Engine Room.
    - `factory_config.json`: The "Brain" of the site (Config).
    - `scripts/`: Automation tools (Audi, Generation, Deployment).
- `images/`: High-fidelity WebP/JPG assets.

## Deployment Protocol
**NEVER** modify S3 manually. Always use the deployment script to ensure synchronization and cache invalidation.

```bash
# 1. Audit
python3 factory/scripts/preflight_audit.py

# 2. Deploy
bash deploy.sh
```

## Content Updates
To add new blog posts:
1. Edit `factory/scripts/generate_blog_batch.py` to add new articles.
2. Run `python3 factory/scripts/generate_blog_batch.py`.
3. Run `deploy.sh`.

## Security & Performance
- **SSL:** Managed by AWS Certificate Manager (via CloudFront).
- **CDN:** Global edge caching via CloudFront.
- **Privacy:** `privacy/index.html` and `terms/index.html` compliant.

---
*Generated by the Website Factory Engine - Jan 2026*
